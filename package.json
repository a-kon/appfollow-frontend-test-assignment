{
  "name": "appfollow_frontend_test_task",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "jest --config=./jest.config.json",
    "watch": "jest --watch --notify --config=./jest.config.json"
  },
  "author": "a-kon",
  "license": "ISC",
  "devDependencies": {
    "jest": "24.5.0"
  }
}
