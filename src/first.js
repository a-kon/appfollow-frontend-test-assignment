// Дан массив из целых чисел
// Написать функцию findOdd, нужно найти и вернуть единственное число, встречающееся в массиве нечетное количество раз
// Всегда будет только одно число, встречающееся нечетное количество раз
// Массив приходит всегда и имеет значения. Элементы массива - только целые числа

function findOdd(array) {
    // Ваш код

    return;
}

module.exports = findOdd;
